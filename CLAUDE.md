# 뚱냥만두 — 프로젝트 컨텍스트 (Claude Code용)

작성 기준일: 2026-09-09 (출시 완료)

## 한 줄 요약

만두속 → 반죽 → 모양+조리법을 골라 만두를 만드는 캐주얼 미니게임.
정답 레시피는 단 하나이고, 광고를 봐야 재료를 하나씩 알아낼 수 있어요.
토스 앱인토스(Apps in Toss) 미니앱, WebView SDK(`@apps-in-toss/web-framework`) + Vite + React.
창의적인 오답은 고양이가 "신메뉴"로 등재해 도감에 넣어줘요.

## 지금 상태

- **콘솔 등록 완료.** workspaceId 61095(`doran`) / miniAppId 71595 / appName `mandu-game`
  콘솔 홈: https://apps-in-toss.toss.im/workspace/61095/mini-app/71595/home
- **출시 완료(2026-09-09).** `serviceStatus: OPEN` / 라이브 번들 `20260909-12`.
  서비스 링크 `intoss://mandu-game`
  반려 이력: `-10` 중복 버튼 / `-11` 광고 예고 부족 (아래 결정 사항 참고).
  다음 번들도 같은 순서다 — `bundle_upload` → `bundle_test_push` → `bundle_submit_review` →
  승인 후 **콘솔 웹 '앱 출시 > 출시하기'** (라이브 전환은 MCP로 불가)
- **광고 지면 활성(`ENABLED`).** 배너·보상형 각각 하나. 실기기에서 노출·재생 확인 완료.
  **ID는 저장소에 두지 않는다** — `.env`(gitignore)에 넣고 `src/ads/policy.ts`가 `import.meta.env`로 읽는다.
  값은 `iaa_placement_group_list`로 확인한다. 지면 이름은 워크스페이스 단위로 유일해야 한다
  (털뽑기가 '하단 광고'를 이미 써서 앱 이름을 앞에 붙였다)
- **앱 정보 최신.** 이름 뚱냥만두 / 아이콘 뚱냥이 / 상세설명·키워드 새 구조 반영 / 세로 스크린샷 4장 등록
- GitHub: https://github.com/caosJeong/MANDU-GAME (public)
- 기획서 원본: `~/Downloads/만두게임_기획서_1.md` (보완본. `만두게임_기획서.md`는 구버전)

## 확정된 결정 사항

- **비게임(생활>일상>취미)으로 콘솔 등록 — 미해결 리스크.**
  게임 카테고리는 게임물 자체등급분류 번호·사업자명·가로썸네일이 전부 필수라 당장 등록이 불가능해서
  `hair-plucking-app`과 같은 경로로 등록했다(2026-09-01). **이건 플랫폼 정책에 어긋난다.**

  공지 51523 「게임물 분류에 따른 운영 정책 변경 안내」(2026-07-22, 조치 필요):
  - 게임물에 해당하는 미니앱은 **반드시 게임 카테고리**로 등록해야 한다
  - 비게임으로 등록된 게임물은 **게임 카테고리의 신규 앱으로 재등록**해야 한다.
    콘솔은 카테고리 직접 변경을 지원하지 않는다 — `miniapp_update_category`로 이전 가능하다고
    적어뒀던 것은 사실과 다르다(2026-09-09 정정)
  - 기존 비게임 카테고리의 해당 미니앱은 **유예기간 종료 후 플랫폼에서 일괄 종료**된다
  - 「게임산업진흥에 관한 법률」상 등급분류 없이는 유통·서비스할 수 없고, 카테고리 불일치는 시정·제재 대상

  즉 지금 앱(miniAppId 71595)은 그대로 두면 언젠가 종료되고, 고치려면 **새 앱을 다시 만들어야 한다**
  (appName도 새로 받아야 하므로 `mandu-game`은 못 쓴다). 2026-09-09 이 상태로 검수를 요청했고,
  반려되거나 출시가 막히면 콘솔 채널톡에 문의하기로 했다.
- **앱 헤더는 없애고, 내비게이션 바의 뒤로가기·홈도 끈다.**
  토스가 미니앱 위에 내비게이션 바를 그려줘서 앱 자체 헤더(홈/나가기)는 2026-09-02에 지웠다.
  그런데 내비바가 기본으로 그리는 **뒤로가기·홈**과 앱 안의 `돌아가기`(도감)·`🏠 홈`(결과)이
  겹쳐서 2026-09-09 검수에서 반려됐다:

  > 내비게이션 바의 뒤로가기 버튼과 미니앱 자체 헤더 및 뒤로가기 버튼이 함께 노출돼요.
  > 중복된 버튼을 제거해 주세요.

  **내비바 쪽을 껐다** — `apps-in-toss.config.ts`의 `navigationBar: { withBackButton: false,
  withHomeButton: false }` (`webView` 안이 아니라 **최상위 키**다). 반대로 앱 안 버튼을 지우면 안 된다:
  내비바 뒤로가기는 우리 SPA의 화면 전환을 모르기 때문에(히스토리를 안 쓰는 단일 페이지) 눌러도
  타이틀로 못 돌아오고 앱이 닫혀서, 도감에서 빠져나올 방법이 없어진다.
  닫기(✕)·더보기(⋯)는 내비바에 그대로 남는다.

  기획서 7장의 "모든 화면에서 나가기 경로 확보"는 **기획서가 (참고)로 달아둔 자체 체크리스트**이지
  검증된 토스 정책이 아니다 — 대조 전에는 기획서 7장 항목을 정책으로 단정하지 말 것
- **앱 표시 이름은 "뚱냥만두".** `appName`은 `mandu-game`으로 발급돼 변경 불가.
  아래 단어는 상표 리스크가 있어 앱 이름·문구·에셋·재료명 어디에도 쓰지 않는다:
  `고향만두` · `황진이` · `고양만두` · `에이스` · `옹스짱`. (이 목록은 지우지 말 것 —
  이유를 모르면 다시 쓰게 된다)
- **광고는 노출 전에 눈에 보이게 알린다(2026-09-09 반려 대응).**

  > 유저가 예상하기 어려운 시점에 광고가 노출돼요. 광고 노출 전에 유저가 인지할 수 있도록
  > CTA 문구나 UI를 추가해 주세요.

  `BannerSlot`에 `aria-label="광고 영역"`만 있었는데 이건 **스크린리더 전용**이라 화면에는
  광고 표시가 전혀 없었다. 타이틀(배너 없음) → 플레이로 넘어가면 예고 없이 배너가 나타났다.
  - 배너 위에 **`광고` 라벨을 항상 표시**한다(`.banner-area__label`). 배너가 붙은 뒤에도 남는다
  - 보상형 버튼은 **"광고 영상 보고 힌트 얻기"** + "영상이 끝나면 재료 하나의 양을 알려줘요".
    영상이 재생된다는 점과 보상을 버튼에서 바로 알 수 있게 했다

  반려 문구가 배너인지 보상형인지 명시하지 않아 양쪽 다 손봤다. 또 같은 사유로 반려되면
  보상형 재생 직전 확인 시트를 넣는 것이 다음 수단이다(유일한 수익 지점이라 지금은 보류).
- **전면(인터스티셜) 광고 미사용.** 배너(상시) + 보상형(선택) 이원화만 사용
- **배너 노출 화면**: 타이틀 X / 플레이·결과·도감 O / 로딩·팝업 X
  (`src/ads/policy.ts`의 `BANNER_ALLOWED_SCREENS`가 단일 진실 공급원)
- **주간 랭킹·포인트 시스템 삭제(2026-09-01).** 랭킹은 혼자 보는 로컬 기록이라 재방문 동기가 되지 않았고,
  포인트는 리워드 전환 계획이 없어 의미 없는 숫자였다. 보상형 광고는 **정통 레시피 힌트**로 대체했다
- **타이틀 화면은 로고/시작하기/도감만.** 카운터 타일 없음
- **화면은 스크롤되지 않는다.** `.screen`이 `flex: 1 1 auto`라 신축 요소(그릇·반죽·작업대)가 공간을 흡수한다.
  `1 0 auto`로 두면 줄어들지 못해 패널에 스크롤이 생긴다. `overflow-y: auto`는 아주 작은 화면용 안전장치로 남겨뒀다

## 프로젝트 구조

```
mandu-game/
├── .env                         # 광고 지면 ID (gitignore). .env.example 을 복사해 채운다
├── apps-in-toss.config.ts       # appName(mandu-game, 변경 불가), brand 보라 #B068CC
├── assets/
│   ├── icon-600.png             # 앱 로고 (뚱냥이). 콘솔 반영 완료
│   └── make_icon.py             # 로고 생성 (PIL 없이 zlib PNG 직접 인코딩, SDF 합집합)
├── design/                      # 화면 목업 — *.dc.html 이 원본, mandu-game-screens.html 이 게시본
├── docs/
│   ├── screens.png              # README 용 몽타주
│   └── screenshots/             # 콘솔에 등록한 세로 스크린샷 원본 (636x1048)
├── src/
│   ├── App.tsx                  # 화면 라우팅 + 라운드 결과/힌트 보상 상태
│   ├── platform.ts              # SafeArea, User.getAnonymousKey
│   ├── storage.ts               # localStorage (도감/알아낸 힌트/정통 클리어/userId)
│   ├── components/
│   │   ├── Cat.tsx              # 만두 든 고양이 캐릭터 (표정 4종) + 앞발
│   │   └── Bowl.tsx             # 만두속 그릇 — 다진 재료가 담을수록 차오른다
│   ├── game/
│   │   ├── ingredients.ts       # 재료 14종 (정답 7 + 오답 7), 색·써는 방식 포함
│   │   ├── recipe.ts            # 정통 레시피(수량 포함) · 신메뉴 6종 · judge()
│   │   ├── haptics.ts           # Device.triggerHaptic 래퍼 (브라우저 폴백)
│   │   └── sounds.ts            # Web Audio 합성 효과음 8종
│   ├── stages/                  # 3단계 (전부 선택형, 타이밍 미니게임 없음)
│   │   ├── FillingStage.tsx     # 만두속 — 재료 중복 선택 (수량)
│   │   ├── DoughStage.tsx       # 반죽 3택 → 만두피 두께
│   │   └── ShapeStage.tsx       # 모양 3택 → 모양별 가능한 조리법
│   ├── screens/                 # Title / Play / Result / Collection
│   ├── hooks/                   # useRewardedAd · useTossBanner
│   └── ads/
│       ├── policy.ts            # 광고 지면 ID(.env) + 정책 가드 + 배너 허용 화면
│       ├── bridge.ts            # 토스 브릿지 없는 환경 안전 가드
│       └── BannerSlot.tsx       # 하단 고정 배너 슬롯
└── .claude/launch.json
```

## 게임 규칙 / 주요 수치

- **정통 레시피는 단 하나이고 총 12번의 선택이다.** 구체적인 재료·수량은 여기 적지 않는다 —
  공개 저장소라 정답이 그대로 노출되기 때문이다. 값은 `src/game/recipe.ts`의 `RECIPE`에 있다.
  수량은 **대부분 추정치**라 밸런싱 때 조정이 필요하다
- **판정 순서**: ① 재료 종류+수량 완전 일치 → 정통 ② 신메뉴 조합과 종류만 일치(수량 무관) → 신메뉴 ③ 그 외 실패
- **재료는 중복 선택.** 같은 재료를 여러 번 넣을 수 있고 개수 제한이 없다.
  수량 배지를 눌러 하나만 빼고, 제목 줄의 '비우기'로 전부 지운다
- **오답 재료 7종**: 계란·초콜릿·매운고추·치즈·고추장·마요네즈·젤리
- **신메뉴 6종**: 매콤치즈/에그마요/디저트/담백채식/불맛/고양이도놀란
- **반죽·모양·조리법은 맛에 영향 없음.** 완성된 만두의 생김새만 정한다.
  모양마다 가능한 조리법이 다르다 (긴반달=굽기 / 또아리=찌기·삶기 / 쭈그리=삶기)
- **광고**: 1회 시청 = 정통 재료 하나의 양 공개. 최대 7회, 저장되어 영구 유지.
  재료 고르는 중에는 정답을 알려주지 않고 **만두가 완성된 뒤 결과 화면에서만** 제안하며,
  광고를 본 직후 무엇을 얻었는지 그 자리에서 보여준다
- **효과음 8종**: tap 버튼 · toss 재료 담기(슝) · undo 빼기 · pick 보기 선택 · roll 반죽 밀기 ·
  success 정통 · newmenu 신메뉴 · fail 실패

## 검증 방법

판정 로직은 번들해서 바로 돌려볼 수 있다:

```bash
npx esbuild src/game/recipe.ts --bundle --format=esm --outfile=/tmp/recipe.mjs
node --input-type=module -e 'import {judge, RECIPE} from "/tmp/recipe.mjs"; console.log(judge({...RECIPE}))'
```

화면은 임시 진입점(`preview.html` + `src/preview.tsx`)을 만들어 각 화면을 한 줄로 렌더하고
헤드리스 크롬으로 스크린샷을 찍은 뒤 지운다. 두 가지를 이 방식으로 잡았다:

- **스크롤 여부**는 눈으로 보지 말고 재라 — 프레임에 `.panel`의 `scrollHeight - clientHeight`를
  찍어두면 어느 화면이 몇 px 넘치는지 바로 보인다
- **스크린샷을 규격대로 뽑을 때** `.app`의 `margin: 0 auto`가 문서 폭 기준으로 가운데 정렬돼
  화면이 밀리고 잘린다. 고정 폭 컨테이너로 감싸고 `margin: 0`을 덮어쓸 것.
  콘솔 규격(세로 636x1048)은 **1px만 달라도 거부**된다

## 다음 할 일

1. **운영 지표 확인** — `dashboard_dau`·`dashboard_retention`·`iaa_report_performance`로
   유입과 광고 성과를 본다. 힌트 광고가 실제로 눌리는지가 핵심 지표다
2. **게임 카테고리 문제** — 세 번의 검수 모두 이 사유가 아니었지만 리스크는 그대로다.
   공지상 유예기간 종료 후 종료 대상이라, 지적받기 전에 채널톡으로 먼저 확인하는 편이 낫다
3. **레시피 수량 검증** — 지금 값은 추정치라 실제 배합비 기준으로 조정 필요
4. 밸런싱: 신메뉴 최종 개수, 정통 레시피 난이도(총 12번이 적절한지).
   실패율이 너무 높으면 이탈로 이어진다 — 지표 보고 판단
5. 개인정보처리방침 등록 (아직 미등록)

## 로컬 개발

```bash
npm install
cp .env.example .env   # 광고 지면 ID (없어도 실행된다)
npm run dev            # http://localhost:5174
```

로컬 브라우저에는 토스 브릿지가 없어서 광고는 동작하지 않고 햅틱은 `navigator.vibrate`로 대체된다.
게임 로직은 그대로 테스트할 수 있다. 정답을 바로 넣어보려면 `src/game/recipe.ts`의 `RECIPE`를 참고한다.
