# 뚱냥만두 — 프로젝트 컨텍스트 (Claude Code용)

작성 기준일: 2026-09-01

## 한 줄 요약

만두속 → 반죽 → 모양+조리법을 골라 만두를 만드는 캐주얼 미니게임.
정답 레시피는 단 하나이고, 광고를 봐야 재료를 하나씩 알아낼 수 있어요.
토스 앱인토스(Apps in Toss) 미니앱, WebView SDK(`@apps-in-toss/web-framework`) + Vite + React.
2000년대 초 플래시 게임 '고향만두' 계승 — 정답 7개를 정확히 골라야 하는 구조와 색감까지.
창의적인 오답은 고양이가 "신메뉴"로 등재해 도감에 넣어줘요.

## 지금 상태

- **로컬 구현 완료, 콘솔 등록 완료(2026-09-01).** workspaceId 61095(`doran`) / miniAppId 71595 / appName `mandu-game`.
  앱 정보(로고·부제·상세설명·카테고리) 등록 후 `miniapp_meta_status`가 `APPROVED`. 아직 번들 미배포(`status: PREPARE`).
  콘솔 홈: https://apps-in-toss.toss.im/workspace/61095/mini-app/71595/home
- 기획서 원본: `~/Downloads/만두게임_기획서_1.md` (보완본. `만두게임_기획서.md`는 구버전)
- 빌드 통과(`npm run build`), 로컬에서 전체 게임 루프 1판 완주 검증 완료
  (주문 4개 소화 → 히든 3종 발견 → 도감 3/8 등록 → 225P 지급 → localStorage 저장 확인)
- **광고 지면 발급 완료(2026-09-09).** 배너·보상형 각각 하나씩. **ID는 저장소에 두지 않는다** —
  `.env`(gitignore)에 넣고 `src/ads/policy.ts`가 `import.meta.env`로 읽는다. 값은 콘솔이나
  `iaa_placement_group_list`로 확인한다. `.env.example`을 복사해 채우면 된다.
  생성 직후 `state=REGISTERING`(구글 반영 중, 최대 2시간)이라 그 전에는 광고가 나오지 않는다.
  `iaa_placement_group_list`로 `state=ENABLED`·`groupId` 채워짐을 한 번 확인할 것 — 반복 폴링 금지.
  지면 이름은 워크스페이스 단위로 유일해야 한다(털뽑기가 '하단 광고'를 이미 써서 앱 이름을 앞에 붙였다).
- **콘솔 상세설명이 현재 코드와 불일치.** 등록 시 쓴 문구에 랭킹 화면과 "3~5분이면 끝나는 짧은 게임"이
  남아 있다 — 아이콘(고양이)과 함께 `miniapp_create`로 갱신 필요.

## 확정된 결정 사항

기획서 4.5에 명시된 것 + 구현하며 정한 것.

- **앱 안에 나가기/홈 버튼을 두지 않는다(2026-09-02).** 토스가 미니앱 위에 내비게이션 바를 그려주고
  거기에 뒤로가기·홈·닫기가 있다 (`withBackButton`/`withHomeButton`, 공지 51389).
  기획서 7장의 "모든 화면에서 나가기 경로 확보"는 **기획서가 (참고)로 달아둔 자체 체크리스트**이지
  검증된 토스 정책이 아니다 — 공식 문서와 대조하지 않고 확정 정책처럼 옮겨 적었던 것을 정정한다.
  대조 전에는 기획서 7장 항목을 정책으로 단정하지 말 것
- **비게임(생활>일상>취미)으로 콘솔 등록 — 미해결 리스크.**
  게임 카테고리는 게임물 자체등급분류 번호·사업자명·가로썸네일이 전부 필수라 당장 등록이 불가능해서
  `hair-plucking-app`과 같은 경로로 등록했다(2026-09-01). **이건 플랫폼 정책에 어긋난다.**

  공지 51523 「게임물 분류에 따른 운영 정책 변경 안내」(2026-07-22, 조치 필요):
  - 게임물에 해당하는 미니앱은 **반드시 게임 카테고리**로 등록해야 한다
  - 비게임으로 등록된 게임물은 **게임 카테고리의 신규 앱으로 재등록**해야 한다.
    콘솔은 카테고리 직접 변경을 지원하지 않는다 — `miniapp_update_category`로 이전 가능하다고
    적어뒀던 것은 사실과 다르다(2026-09-09 정정)
  - 기존 비게임 카테고리의 해당 미니앱은 **유예기간 종료 후 플랫폼에서 일괄 종료**된다
  - 「게임산업진흥에 관한 법률」상 등급분류 없이는 유통·서비스할 수 없고, 카테고리 불일치는 시정·제재 대상

  즉 지금 앱(miniAppId 71595)은 그대로 두면 언젠가 종료되고, 고치려면 **새 앱을 다시 만들어야 한다**
  (appName도 새로 받아야 하므로 `mandu-game`은 못 쓴다). 2026-09-09 이 상태로 검수를 요청했고,
  반려되거나 출시가 막히면 콘솔 채널톡에 문의하기로 했다.
- **전면(인터스티셜) 광고 미사용.** 배너(상시) + 보상형(선택) 이원화만 사용
- **배너 노출 화면**: 타이틀 X / 플레이·결과·도감 O / 로딩·팝업 X (`src/ads/policy.ts`의 `BANNER_ALLOWED_SCREENS`가 단일 진실 공급원)
- **재료 선택 2~4개.** 기획 초안은 3~4개였으나 히든 레시피에 2개 조합(초코+딸기, 사과+계피)이 있어 최소 2개
- **주간 랭킹 삭제(2026-09-01).** 혼자 보는 로컬 기록이라 재방문 동기가 되지 않았다
- **포인트 시스템 삭제(2026-09-01).** 리워드 전환 계획이 없어 의미 없는 숫자였다.
  보상형 광고 유인이 "다음 판 재료 슬롯 +1" 하나만 남았으니 보상 설계 재검토 필요
- **타이틀 화면은 로고/시작하기/도감만.** 포인트·히든만두 카운터 타일 제거
- **앱 표시 이름은 "뚱냥만두"(2026-09-01).** `appName`은 `mandu-game`으로 발급돼 변경 불가.
  `고향만두`·`황진이`·`고양만두`는 상표 리스크로 쓰지 않는다 (원작 재료 중 에이스·옹스짱도 같은 이유로 교체)
- **히든 도감 등록은 광고와 무관하게 자동.** 기획서 4.3의 "영상 보고 도감 확정 등록"은 3.3의 "자동 등록"과 충돌해서 등록을 자동으로 했다. 포인트 삭제로 이 광고 슬롯 자체가 없어졌다.
- **히든 레시피 8종으로 시작.** 기획서 8번 "몇 개로 시작할지" 미확정 상태의 잠정치

## 프로젝트 구조

```
mandu-game/
├── apps-in-toss.config.ts      # appName(mandu-game, 변경 불가), brand 보라 #B068CC
├── assets/
│   ├── icon-600.png             # 앱 로고 (뚱냥이). 콘솔 반영 전
│   └── make_icon.py             # 로고 생성 스크립트 (PIL 없이 zlib PNG 직접 인코딩, SDF 합집합)
├── design/                      # 화면 목업 — *.dc.html 이 원본, mandu-game-screens.html 이 게시본
├── src/
│   ├── App.tsx                  # 화면 라우팅 + 라운드 결과/힌트 광고 상태
│   ├── platform.ts              # SafeArea, User.getAnonymousKey
│   ├── storage.ts               # localStorage (도감/알아낸 힌트/정통 클리어/userId)
│   ├── components/Cat.tsx       # 만두 든 고양이 캐릭터 (표정 4종) + 앞발
│   ├── game/
│   │   ├── ingredients.ts        # 재료 14종 (정답 7 + 오답 7)
│   │   ├── recipe.ts             # 정통 레시피(수량 포함) · 신메뉴 6종 · judge()
│   │   ├── haptics.ts            # Device.triggerHaptic 래퍼 (브라우저 폴백)
│   │   └── sounds.ts             # Web Audio 합성 효과음
│   ├── stages/                   # 3단계 (전부 선택형, 타이밍 미니게임 없음)
│   │   ├── FillingStage.tsx      # 만두속 — 재료 중복 선택 (수량)
│   │   ├── DoughStage.tsx        # 반죽 3택 → 만두피 두께
│   │   └── ShapeStage.tsx        # 모양 3택 → 모양별 가능한 조리법
│   ├── screens/                  # Title / Play / Result / Collection
│   └── ads/
│       ├── policy.ts             # 광고 그룹 ID + 정책 가드 + 배너 허용 화면
│       ├── bridge.ts             # 토스 브릿지 없는 환경 안전 가드
│       └── BannerSlot.tsx        # 하단 고정 배너 슬롯
└── .claude/launch.json
```

## 게임 규칙 / 주요 수치

- **정통 레시피(단 하나)**: 돼지고기 ×3 · 두부 ×2 · 양파 ×2 · 부추 ×2 · 대파 ×1 · 마늘 ×1 · 당면 ×1 = 총 12번.
  실제 해태 고향만두 표시 원재료의 함량 내림차순 순서를 12번의 선택으로 환산한 값이다.
  **공개된 함량 수치는 돼지고기 13% 하나뿐**이라 나머지는 추정치 — 제품 패키지 확인 후 조정 필요
- **판정 순서**: ① 재료 종류+수량 완전 일치 → 정통 ② 신메뉴 조합과 종류만 일치(수량 무관) → 신메뉴 ③ 그 외 실패
- **오답 재료 7종**: 계란·초콜릿·매운고추·치즈·고추장·마요네즈·젤리
  (원작의 에이스·옹스짱은 브랜드 자산이라 초콜릿·젤리로 교체)
- **신메뉴 6종**: 매콤치즈/에그마요/디저트/담백채식/불맛/고양이도놀란
- **반죽·모양·조리법은 맛에 영향 없음.** 완성된 만두의 생김새만 정한다 (모양별 가능한 조리법은 원작 그대로)
- **광고**: 1회 시청 = 정통 재료 하나의 양 공개. 최대 7회, 저장되어 영구 유지.
  재료 고르는 중에는 정답을 알려주지 않고 **만두가 완성된 뒤 결과 화면에서만** 제안한다

## 검증 방법

타이밍 미니게임이 사라져서 예전에 필요하던 `requestAnimationFrame` 폴리필은 이제 필요 없다.

판정 로직은 번들해서 바로 돌려볼 수 있다:

```bash
npx esbuild src/game/recipe.ts --bundle --format=esm --outfile=/tmp/recipe.mjs
node --input-type=module -e 'import {judge, RECIPE} from "/tmp/recipe.mjs"; console.log(judge({...RECIPE}))'
```

화면은 임시 진입점(`preview.html` + `src/preview.tsx`)을 만들어 각 화면을 한 줄로 렌더한 뒤
헤드리스 크롬으로 스크린샷을 찍고 지우는 식으로 확인했다.

## 다음 할 일 (우선순위 순)

1. **콘솔 갱신** — 표시 이름(뚱냥만두)·상세설명·아이콘이 전부 옛날 것이다.
   `miniapp_create`(miniAppId=71595)로 한 번에 갱신할 것. 상세설명에 아직 랭킹과 포인트 얘기가 남아 있다
2. **레시피 검증** — 제품 패키지 원재료명을 확인해 실제 배합비로 조정 (지금은 표시 순서 기반 추정치)
3. **광고 반영 확인** → 생성 후 2시간 지나 `iaa_placement_group_list`로 `ENABLED` 확인,
   그 뒤 실기기에서 배너 노출·힌트 보상 지급까지 확인
4. 밸런싱: 신메뉴 최종 개수, 정통 레시피 난이도(총 12번이 적절한지)
5. 스크린샷·개인정보처리방침 준비 → 앱 정보 검토 요청
6. 승인 후 `npm run build` → `npx ait build` → `bundle_upload` → QR 테스트 → 출시

## 로컬 개발

```bash
npm install
npm run dev   # http://localhost:5174
```

로컬 브라우저에는 토스 브릿지가 없어서 광고/햅틱/나가기는 자동으로 비활성화(햅틱은 navigator.vibrate 폴백)된다.
게임 로직은 그대로 테스트 가능하다.
